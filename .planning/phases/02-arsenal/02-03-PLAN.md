---
phase: 02-arsenal
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Source/RevenantOps/Weapons/WeaponMelee.h
  - Source/RevenantOps/Weapons/WeaponMelee.cpp
autonomous: false
requirements: [ARME-06]

must_haves:
  truths:
    - "L'arme de melee attaque au corps-a-corps sans consommer de munitions"
    - "Le melee utilise un sphere trace au lieu de hitscan"
    - "Le melee fait 35 degats avec une portee de 100cm"
    - "Le melee ne recharge jamais et a un magazine infini"
  artifacts:
    - path: "Source/RevenantOps/Weapons/WeaponMelee.h"
      provides: "Melee weapon subclass with FireShot override"
      contains: "class AWeaponMelee"
    - path: "Source/RevenantOps/Weapons/WeaponMelee.cpp"
      provides: "SphereTrace melee implementation"
      contains: "SweepSingleByChannel"
  key_links:
    - from: "WeaponMelee::FireShot"
      to: "WeaponBase::FireShot"
      via: "virtual override"
      pattern: "void AWeaponMelee::FireShot"
    - from: "WeaponMelee::FireShot"
      to: "SphereTrace"
      via: "SweepSingleByChannel"
      pattern: "SweepSingleByChannel"
---

<objective>
Create the melee weapon that overrides FireShot() to use sphere trace instead of hitscan, with infinite ammo. Then verify all 6 weapons compile.

Purpose: The melee weapon is the risk/reward option — high damage (35) but requires close range contact. It overrides the virtual FireShot() (made virtual in Plan 01) to perform a sphere trace sweep instead of line trace hitscan.
Output: Compilable WeaponMelee C++ subclass with sphere trace attack. All 6 weapons verified in editor.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-arsenal/02-CONTEXT.md
@.planning/phases/02-arsenal/02-01-SUMMARY.md

<interfaces>
<!-- FireShot is virtual after Plan 01. Melee overrides it. -->

From Source/RevenantOps/Weapons/WeaponBase.h (after Plan 01):
```cpp
protected:
  virtual void FireShot(); // Now virtual — can be overridden

  // State management used by FireShot:
  void SetWeaponState(EWeaponState NewState);
  float LastFireTime;
  bool bWantsToFire;
  int32 CurrentAmmo;
  int32 MagazineSize;
  AController* OwnerController;
  APawn* OwnerPawn;

  // Blueprint hooks called during fire:
  void BP_OnFire(const FVector& MuzzleLocation, const FRotator& MuzzleRotation);
  void BP_OnHit(const FHitResult& HitResult, float DamageDealt);

  // Delegates:
  FOnAmmoChanged OnAmmoChanged;
```

Melee sphere trace reference (from Variant_Combat CombatCharacter):
```cpp
// SphereTrace: 75cm distance, 75cm radius
// For melee weapon: 100cm distance, 50cm radius (per CONTEXT.md)
```

Key: Melee FireShot must NOT consume ammo (MagazineSize = 999, skip ammo decrement),
must use SweepSingleByChannel with FCollisionShape::MakeSphere instead of LineTraceSingleByChannel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeaponMelee with sphere trace FireShot override</name>
  <files>Source/RevenantOps/Weapons/WeaponMelee.h, Source/RevenantOps/Weapons/WeaponMelee.cpp</files>
  <read_first>Source/RevenantOps/Weapons/WeaponBase.h, Source/RevenantOps/Weapons/WeaponBase.cpp</read_first>
  <action>
1. WeaponMelee.h:
   - `#pragma once`, include WeaponBase.h
   - `UCLASS(Blueprintable)` class AWeaponMelee : public AWeaponBase
   - Constructor declaration
   - `virtual void FireShot() override;` in protected section
   - UPROPERTY for MeleeRange (float, EditDefaultsOnly, default 100.f) — trace distance in cm
   - UPROPERTY for MeleeRadius (float, EditDefaultsOnly, default 50.f) — sphere trace radius in cm

2. WeaponMelee.cpp constructor — set defaults:
   - WeaponName = "Machette"
   - WeaponCategory = EWeaponCategory::Pistol (no Melee enum exists — use Pistol as placeholder, per Claude's discretion from CONTEXT.md)
   - FireMode = EWeaponFireMode::SemiAuto
   - FireRate = 120.f (2 swings per second)
   - BaseDamage = 35.f (high risk/reward)
   - PelletsPerShot = 1
   - MagazineSize = 999 (infinite — never runs out)
   - CurrentAmmo = 999
   - MaxReserveAmmo = 0 (no reserve needed)
   - CurrentReserveAmmo = 0
   - ReloadTime = 0.f (no reload)
   - BaseSpread = 0.f, MaxSpread = 0.f, SpreadPerShot = 0.f (no spread)
   - VerticalRecoil = 0.f, HorizontalRecoilRange = 0.f (no recoil)
   - ADSFOV = 90.f (no zoom)
   - ADSMoveSpeedMultiplier = 1.0f (full speed in melee)
   - MaxRange = 100.f (cosmetic — actual range is MeleeRange)
   - MeleeRange = 100.f
   - MeleeRadius = 50.f

3. WeaponMelee.cpp FireShot() override:
   - Do NOT call Super::FireShot() — completely replace the hitscan logic
   - Do NOT decrement CurrentAmmo (infinite ammo — but still set it to 999 each shot to prevent auto-reload edge cases)
   - Set LastFireTime = GetWorld()->GetTimeSeconds()
   - SetWeaponState(EWeaponState::Firing)
   - Get trace origin: use OwnerPawn->GetActorLocation() + OwnerPawn->GetActorForwardVector() as direction
   - TraceStart = OwnerPawn->GetActorLocation() (at character center)
   - TraceEnd = TraceStart + OwnerPawn->GetActorForwardVector() * MeleeRange
   - FCollisionShape SphereShape = FCollisionShape::MakeSphere(MeleeRadius)
   - FCollisionQueryParams: ignore self, ignore OwnerPawn
   - GetWorld()->SweepSingleByChannel(HitResult, TraceStart, TraceEnd, FQuat::Identity, ECC_Visibility, SphereShape, QueryParams)
   - If hit: calculate damage (BaseDamage, no falloff for melee), apply via TakeDamage with FPointDamageEvent, call BP_OnHit
   - Call BP_OnFire with OwnerPawn location and rotation
   - Handle fire mode: bWantsToFire = false (SemiAuto behavior)
   - SetWeaponState(EWeaponState::Idle)
   - Broadcast OnAmmoChanged(CurrentAmmo, MagazineSize) to keep HUD consistent
  </action>
  <acceptance_criteria>
    - grep: "class AWeaponMelee : public AWeaponBase" in WeaponMelee.h
    - grep: "virtual void FireShot() override" in WeaponMelee.h
    - grep: "MeleeRange" in WeaponMelee.h
    - grep: "MeleeRadius" in WeaponMelee.h
    - grep: "SweepSingleByChannel" in WeaponMelee.cpp
    - grep: "BaseDamage = 35" in WeaponMelee.cpp
    - grep: "MagazineSize = 999" in WeaponMelee.cpp
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Noureddine/Documents/RevenantOps" && grep -q "FireShot.*override" Source/RevenantOps/Weapons/WeaponMelee.h && grep -q "SweepSingleByChannel" Source/RevenantOps/Weapons/WeaponMelee.cpp && grep -q "BaseDamage = 35" Source/RevenantOps/Weapons/WeaponMelee.cpp && grep -q "MagazineSize = 999" Source/RevenantOps/Weapons/WeaponMelee.cpp && echo "PASS"</automated>
  </verify>
  <done>WeaponMelee compiles with sphere trace override (100cm range, 50cm radius), 35 dmg, infinite ammo, no reload/recoil/spread.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all 6 weapons compile in UE5.7</name>
  <files>Source/RevenantOps/Weapons/WeaponMelee.h</files>
  <action>All 6 weapon C++ subclasses have been created across Plans 01-03. The user must compile the project in UE5.7 editor to verify no compilation errors and that all classes appear as Blueprint parent classes.</action>
  <what-built>6 weapon C++ subclasses (Pistol, AR, SMG, Shotgun, Sniper, Melee) all inheriting from WeaponBase with distinct stats and behaviors. Melee overrides FireShot with sphere trace.</what-built>
  <how-to-verify>
    1. Open UE5.7 editor, compile the project (Ctrl+Shift+B or Build from toolbar)
    2. Verify no compilation errors
    3. In Content Browser, right-click > Blueprint Class > search for each weapon class:
       - WeaponPistol, WeaponAssaultRifle, WeaponSMG, WeaponShotgun, WeaponSniper, WeaponMelee
       - All 6 should appear as available parent classes
    4. (Optional) Create a Blueprint from WeaponPistol, open Details panel, verify stats visible (FireRate, BaseDamage, etc.)
    5. (Optional) In BP_ThirdPersonCharacter, add weapon Blueprint classes to DefaultWeaponClasses array and test switching in PIE
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/Noureddine/Documents/RevenantOps" && ls Source/RevenantOps/Weapons/Weapon*.h | wc -l | grep -q "[7-9]" && echo "PASS: All weapon headers exist"</automated>
  </verify>
  <done>All 6 weapon classes compile in UE5.7 and appear as Blueprint parent classes. User confirmed.</done>
  <resume-signal>Type "approved" or describe compilation errors / issues</resume-signal>
</task>

</tasks>

<verification>
WeaponMelee.h declares FireShot override. WeaponMelee.cpp uses SweepSingleByChannel instead of LineTraceSingleByChannel. MagazineSize is 999 (infinite). All 6 weapon classes compile and appear in UE5.7 editor as Blueprint parent classes.
</verification>

<success_criteria>
- WeaponMelee overrides FireShot with sphere trace (100cm range, 50cm radius)
- Melee does 35 damage, has infinite ammo (999), no reload, no recoil, no spread
- All 6 weapon C++ classes compile without errors in UE5.7
- All 6 classes visible as Blueprint parent classes in editor
</success_criteria>

<output>
After completion, create `.planning/phases/02-arsenal/02-03-SUMMARY.md`
</output>
