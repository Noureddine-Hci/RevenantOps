---
phase: 02-arsenal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/RevenantOps/Weapons/WeaponShotgun.h
  - Source/RevenantOps/Weapons/WeaponShotgun.cpp
  - Source/RevenantOps/Weapons/WeaponSniper.h
  - Source/RevenantOps/Weapons/WeaponSniper.cpp
autonomous: true
requirements: [ARME-03, ARME-04]

must_haves:
  truths:
    - "Le shotgun tire 8 pellets en spread avec 15 degats par pellet"
    - "Le sniper tire un seul projectile a 120 degats avec zoom ADSFOV 30"
    - "Le shotgun a un rechargement lent (3.0s) et un magazine de 6"
    - "Le sniper a un magazine de 5 et une cadence tres lente (40 RPM)"
  artifacts:
    - path: "Source/RevenantOps/Weapons/WeaponShotgun.h"
      provides: "Shotgun weapon subclass"
      contains: "class AWeaponShotgun"
    - path: "Source/RevenantOps/Weapons/WeaponSniper.h"
      provides: "Sniper weapon subclass"
      contains: "class AWeaponSniper"
  key_links:
    - from: "WeaponShotgun"
      to: "WeaponBase"
      via: "C++ inheritance"
      pattern: "public AWeaponBase"
    - from: "WeaponSniper"
      to: "WeaponBase"
      via: "C++ inheritance"
      pattern: "public AWeaponBase"
---

<objective>
Create Shotgun and Sniper weapon subclasses with their unique characteristics (multi-pellet spread and high-damage zoom respectively).

Purpose: These two weapons have the most distinctive behaviors — shotgun uses PelletsPerShot > 1 for spread damage, sniper has lowest ADSFOV for zoom. They complete the ranged arsenal.
Output: 2 compilable weapon C++ subclasses.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-arsenal/02-CONTEXT.md

<interfaces>
<!-- WeaponBase properties relevant to Shotgun and Sniper -->

From Source/RevenantOps/Weapons/WeaponBase.h:
```cpp
// Shotgun uses PelletsPerShot > 1 — WeaponBase::FireShot loops PelletsPerShot times
int32 PelletsPerShot = 1; // Set to 8 for shotgun

// Sniper uses low ADSFOV for zoom
float ADSFOV = 65.f; // Set to 30 for sniper zoom

// Key damage falloff properties for shotgun (short range) vs sniper (long range)
float DamageFalloffStart = 2000.f;
float DamageFalloffEnd = 5000.f;
float MinDamageMultiplier = 0.4f;
```

Subclass pattern (constructor defaults only, no method overrides needed):
```cpp
AWeaponSubclass::AWeaponSubclass() {
  WeaponName = FText::FromString(TEXT("Name"));
  WeaponCategory = EWeaponCategory::Shotgun;
  // ... set all stats
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WeaponShotgun</name>
  <files>Source/RevenantOps/Weapons/WeaponShotgun.h, Source/RevenantOps/Weapons/WeaponShotgun.cpp</files>
  <read_first>Source/RevenantOps/Weapons/WeaponBase.h</read_first>
  <action>
Create WeaponShotgun following the same subclass pattern (constructor defaults only).

WeaponShotgun.h:
- `#pragma once`, include WeaponBase.h
- `UCLASS(Blueprintable)` class AWeaponShotgun : public AWeaponBase
- Constructor declaration only

WeaponShotgun.cpp — constructor sets per CONTEXT.md:
- WeaponName = "Shotgun"
- WeaponCategory = EWeaponCategory::Shotgun
- FireMode = EWeaponFireMode::SemiAuto
- FireRate = 80.f (very slow between shots)
- BaseDamage = 15.f (per pellet)
- PelletsPerShot = 8 (THIS is what makes it a shotgun — WeaponBase loops 8 hitscan traces)
- MagazineSize = 6, CurrentAmmo = 6
- MaxReserveAmmo = 24, CurrentReserveAmmo = 24
- ReloadTime = 3.0f (slow pump reload)
- BaseSpread = 5.0f, MaxSpread = 8.f, SpreadPerShot = 0.5f (wide spread cone)
- VerticalRecoil = 2.5f, HorizontalRecoilRange = 1.0f (heavy kick)
- ADSRecoilMultiplier = 0.7f
- ADSFOV = 65.f (no zoom advantage)
- ADSMoveSpeedMultiplier = 0.5f
- MaxRange = 3000.f (short effective range)
- DamageFalloffStart = 500.f, DamageFalloffEnd = 1500.f (damage drops fast)
- MinDamageMultiplier = 0.2f (nearly useless at range)
- HeadshotMultiplier = 1.5f (lower headshot bonus due to pellet spread)
  </action>
  <acceptance_criteria>
    - grep: "class AWeaponShotgun : public AWeaponBase" in WeaponShotgun.h
    - grep: "PelletsPerShot = 8" in WeaponShotgun.cpp
    - grep: "BaseDamage = 15" in WeaponShotgun.cpp
    - grep: "ReloadTime = 3" in WeaponShotgun.cpp
    - grep: "MaxRange = 3000" in WeaponShotgun.cpp
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Noureddine/Documents/RevenantOps" && grep -q "class AWeaponShotgun" Source/RevenantOps/Weapons/WeaponShotgun.h && grep -q "PelletsPerShot = 8" Source/RevenantOps/Weapons/WeaponShotgun.cpp && grep -q "BaseDamage = 15" Source/RevenantOps/Weapons/WeaponShotgun.cpp && echo "PASS"</automated>
  </verify>
  <done>WeaponShotgun compiles with 8 pellets, 15 dmg each, SemiAuto 80 RPM, short range falloff, 3s reload.</done>
</task>

<task type="auto">
  <name>Task 2: Create WeaponSniper</name>
  <files>Source/RevenantOps/Weapons/WeaponSniper.h, Source/RevenantOps/Weapons/WeaponSniper.cpp</files>
  <read_first>Source/RevenantOps/Weapons/WeaponShotgun.h, Source/RevenantOps/Weapons/WeaponShotgun.cpp</read_first>
  <action>
Create WeaponSniper following the same subclass pattern.

WeaponSniper.h:
- `#pragma once`, include WeaponBase.h
- `UCLASS(Blueprintable)` class AWeaponSniper : public AWeaponBase
- Constructor declaration only

WeaponSniper.cpp — constructor sets per CONTEXT.md:
- WeaponName = "Sniper"
- WeaponCategory = EWeaponCategory::Sniper
- FireMode = EWeaponFireMode::SemiAuto
- FireRate = 40.f (very slow — bolt action feel)
- BaseDamage = 120.f (one-shot potential)
- PelletsPerShot = 1
- MagazineSize = 5, CurrentAmmo = 5
- MaxReserveAmmo = 20, CurrentReserveAmmo = 20
- ReloadTime = 2.5f
- BaseSpread = 0.2f, MaxSpread = 3.f, SpreadPerShot = 1.5f (precise first shot, bloom on spam)
- VerticalRecoil = 3.0f, HorizontalRecoilRange = 0.5f (heavy vertical kick)
- ADSRecoilMultiplier = 0.5f (much more stable when scoped)
- ADSSpreadMultiplier = 0.1f (very tight when scoped)
- ADSFOV = 30.f (strong zoom — THIS is the sniper signature)
- ADSInterpSpeed = 8.f (slightly slower ADS transition)
- ADSMoveSpeedMultiplier = 0.4f (slow while scoped)
- MaxRange = 50000.f (extreme range)
- DamageFalloffStart = 5000.f, DamageFalloffEnd = 30000.f (damage holds at distance)
- MinDamageMultiplier = 0.7f (still hits hard at range)
- HeadshotMultiplier = 3.0f (rewarding precision)
  </action>
  <acceptance_criteria>
    - grep: "class AWeaponSniper : public AWeaponBase" in WeaponSniper.h
    - grep: "BaseDamage = 120" in WeaponSniper.cpp
    - grep: "ADSFOV = 30" in WeaponSniper.cpp
    - grep: "FireRate = 40" in WeaponSniper.cpp
    - grep: "HeadshotMultiplier = 3" in WeaponSniper.cpp
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Noureddine/Documents/RevenantOps" && grep -q "class AWeaponSniper" Source/RevenantOps/Weapons/WeaponSniper.h && grep -q "BaseDamage = 120" Source/RevenantOps/Weapons/WeaponSniper.cpp && grep -q "ADSFOV = 30" Source/RevenantOps/Weapons/WeaponSniper.cpp && echo "PASS"</automated>
  </verify>
  <done>WeaponSniper compiles with 120 dmg, SemiAuto 40 RPM, ADSFOV 30 zoom, long range, 3x headshot multiplier.</done>
</task>

</tasks>

<verification>
Both weapon headers declare correct class inheriting from AWeaponBase. Shotgun has PelletsPerShot=8 for multi-pellet behavior. Sniper has ADSFOV=30 for zoom scope. Stats match CONTEXT.md.
</verification>

<success_criteria>
- WeaponShotgun: SemiAuto, 80 RPM, 15 dmg x 8 pellets, mag 6, reserve 24, reload 3s, short range
- WeaponSniper: SemiAuto, 40 RPM, 120 dmg, mag 5, reserve 20, ADSFOV 30, long range, 3x headshot
- Both follow the constructor-defaults-only pattern from WeaponBase
</success_criteria>

<output>
After completion, create `.planning/phases/02-arsenal/02-02-SUMMARY.md`
</output>
